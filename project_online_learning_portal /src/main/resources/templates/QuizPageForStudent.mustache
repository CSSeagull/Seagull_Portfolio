<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Quizzes</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"/>
    <style>
        body {
            margin: 0;
            background-color: #f9f9f9;
        }

        .ui.menu .item img.logo {
            margin-right: 1.5em;
        }

        .quiz-item {
            font-size: 18px;
            margin-bottom: 20px;
        }

        .ui.cards > .card:hover {
            transform: translateY(-5px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }
    </style>
</head>
<body>

<div class="ui fixed inverted menu" style="height: 8%; font-size: 16px; background-color: #2F365F">
    <div class="ui container">
        <div class="right menu">
            <a href="/login" class="ui red button" style="margin-top: 8px; margin-bottom: 8px;">Logout</a>
        </div>
    </div>
</div>

<div class="ui container" style="margin-top: 100px; max-width: 800px;">
    <h2 class="ui center aligned header" style="color: #2F365F; font-size: 32px;">Quizzes for {{course.name}}</h2>

    <div class="ui relaxed divided list" style="margin-top: 40px;">
        {{#quizWithResults}}
            <div class="item quiz-item">
                <div class="content">
                    {{#result}}
                        <div class="header" style="color: #21ba45;">{{quiz.quizName}} - Grade: {{result}}%</div>
                    {{/result}}
                    {{^result}}
                        <a class="header" href="/{{student.name}}/{{courseId}}/quiz/{{quiz.quizName}}" style="color: #4183c4;">
                            {{quiz.quizName}}
                        </a>
                    {{/result}}
                </div>
            </div>
        {{/quizWithResults}}
    </div>

    <div class="ui divider"></div>
<br>
    <a class="ui teal button" href="javascript:history.back()">← Back</a></div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.js"></script>

</body>
</html>
