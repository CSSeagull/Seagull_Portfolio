<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"
    />
    <style>
        body {
            background-color: #f9f9f9;
            margin: 0;
            padding: 2em 0;
        }
        .pdf-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        iframe {
            border: 1px solid #ddd;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 900px;
            height: 800px;
            margin-left: 10%;
        }
        .ui.button {
            margin-top: 2em;
        }
    </style>
</head>
<body>

<div class="ui container pdf-container">
    <h1 class="ui header center aligned">{{title}}</h1>

    {{#pdfData}}
        <iframe
                src="data:application/pdf;base64,{{pdfData}}"
                allowfullscreen>
        </iframe>
    {{/pdfData}}

    <a class="ui teal button" href="javascript:history.back()">← Back</a>
</div>

</body>
</html>
