<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Enrolled Courses</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"/>
    <style>
        body {
            margin: 0;
            background-color: #f9f9f9;
        }

        .dashboard-card {
            margin-top: 100px;
            text-align: center;
        }

        .ui.cards > .card:hover {
            transform: translateY(-5px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }
    </style>
</head>

<body>


<!-- Navbar -->
<div class="ui fixed inverted menu" style="height: 8%; font-size: 16px; background-color: #2F365F">
    <div class="ui container">
        <div class="right menu">
            <a href="/login" class="ui red button" style="margin-top: 8px; margin-bottom: 8px;">Logout</a>
        </div>
    </div>
</div>
<br>
<br>
<br>
<!-- Main Content -->
<div class="ui raised very padded text container segment dashboard-card" style="max-width: 1200px;">
    <h2 class="ui header" style="color: #2F365F; font-size: 30px;">
        Your Enrolled Courses
    </h2>

    <div class="ui centered stackable cards" style="margin-top: 30px;">
        {{#enrollments}}
            <div class="card" style="width: 350px;">
                <div class="content" style="text-align: center;">
                    <div class="header" style="font-size: 22px;">{{course.name}}</div>
                    <div class="meta" style="margin-top: 5px;">Course Code: {{course.courseId}}</div>
                </div>
                <div class="extra content">
                    <form method="post" action="/drop/{{studentId}}/{{course.courseId}}">
                        <button class="ui red button" style="width: 100%;" type="submit">
                            Drop Course
                        </button>
                    </form>
                </div>
            </div>
        {{/enrollments}}
    </div>
    <br>
    <br>
    <a class="ui green button" href="javascript:history.back()">← Back</a>
</div>

</body>
</html>
