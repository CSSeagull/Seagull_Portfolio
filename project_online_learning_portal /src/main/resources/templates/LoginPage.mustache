<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"/>
    <style>
        body {
            background: #f4f7fa;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }

        .login-box {
            width: 400px;
            background: white;
            padding: 2.5rem;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            border-radius: 15px;
            animation: fadeInUp 0.8s ease forwards;
            opacity: 0;
            transform: translateY(20px);
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h2.ui.header {
            text-align: center;
            margin-bottom: 2rem;
        }
    </style>
</head>
<body>
<div class="login-box">
    <h2 class="ui dividing header">Login</h2>

    <form class="ui form" action="/login" method="post">
        <div class="field">
            <label>Email</label>
            <input type="email" name="email" placeholder="your_email@g.rit.edu" required>
        </div>
        <div class="field">
            <label>Password</label>
            <input type="password" name="password" placeholder="Password" required>
        </div>

        <button class="ui primary fluid button" type="submit">Login</button>

        <div class="ui message" style="text-align: center; margin-top: 1.5rem;">
            New here? <a href="/register"><strong>Register now</strong></a>
        </div>
    </form>

    {{#error}}
        <div class="ui red message" style="margin-top: 1rem;">
            {{error}}
        </div>
    {{/error}}
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script>
    $('.ui.dropdown').dropdown();
</script>
</body>
</html>
